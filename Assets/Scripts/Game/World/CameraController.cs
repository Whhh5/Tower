using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    //public Vector3 m_TargetPositionOffset = Vector3.zero;
    //public Vector3 m_TargetForwardOffset = Vector3.zero;


    //private void Update()
    //{
    //    if (GTools.RefIsNull(GTools.PlayerController?.CurController)) return;

    //    var targetPos = GTools.PlayerController.CurController.CentralPoint.position;
    //    targetPos.y = 0;
    //    transform.position = Vector3.Lerp(transform.position, targetPos + m_TargetPositionOffset, GTools.UpdateDeltaTime * 10);
    //    transform.forward = Vector3.Lerp(transform.forward, m_TargetForwardOffset, GTools.UpdateDeltaTime * 10);
    //}
}
